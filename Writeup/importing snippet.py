

import jax.numpy as jnp
import numpy as np

# initialising the array
array = jnp.array(())
# creating a new array with the modified values
array = array.at[i].set(x)

array = np.array(())
array[i] = x
